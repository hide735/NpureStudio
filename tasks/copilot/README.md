# Copilot 向け作業引継ぎフォルダ

このフォルダには、Copilot / 自動エージェントに作業を引き継ぐための、細かく分割された手順ファイルがあります。
各ファイルは独立して実行でき、実行後に期待される成果物と検証手順を必ず含みます。

運用ルール:
- 各タスクは順番通り（番号順）に実行してください。
- 各ステップ完了後、CI的チェック（lint/簡易起動）を行ってください。
- 実行前に `tasks/README.md` と `docs/概要設計.md` を参照してください。
